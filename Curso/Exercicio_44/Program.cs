
int nLinhas = new int();
int nColunas = new int();

Console.Write("Informa o Número de Linhas/Dimensões da Matriz: ");
nLinhas = int.Parse(Console.ReadLine());
Console.Write("Informa o Número de Colunas da Matriz: ");
nColunas = int.Parse(Console.ReadLine());

int[,] numeros = new int[nLinhas, nColunas];

for (int i = 0; i < numeros.Length; i++)
{
    for (int j = 0; j < numeros.Length; j++)
    {
        if (numeros[i,j] == i && )
        {

        }
    }
}
